Change the concrete syntax of `diff-exp`

from: `-(Expression, Expression)`
to:   `(- Expression Expression)`

Change the concrete syntax of `zero?-exp`

from: `zero? (Expression)`
to:   `(zero? Expression)`
